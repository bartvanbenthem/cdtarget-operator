---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: cdtargets.cnad.gofound.nl
spec:
  group: cnad.gofound.nl
  names:
    kind: CDTarget
    listKind: CDTargetList
    plural: cdtargets
    singular: cdtarget
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CDTarget is the Schema for the cdtargets API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CDTargetSpec defines the desired state of CDTarget
            properties:
              ip:
                description: IP is a slice of string that contains all the CDTarget
                  IPs
                items:
                  type: string
                type: array
              podSelector:
                additionalProperties:
                  type: string
                description: specify the pod selector key value pair
                type: object
            type: object
          status:
            description: CDTargetStatus defines the observed state of CDTarget
            properties:
              policy:
                description: Policy contains the name of the CDTarget network policy
                  this verrifies the CDTarget network policy creation
                type: string
              synced:
                description: Synced compares the IPs specified in the CR with the
                  IPs in the actual CDTarget network policy when both are equal the
                  Synced status is set to TRUE
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
